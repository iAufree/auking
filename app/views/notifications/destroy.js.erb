$(function(){
    $('#notification-<%= @notification.id %>').addClass('animated bounceOutRight');
    $('#notification-<%= @notification.id %>').one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', 
        function(){
        	$(this).remove();
            emojify.run();
        }
    );
});